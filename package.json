{
  "$schema": "https://json.schemastore.org/package.json",
  "name": "@niit/niit-crm",
  "description": "Laravel project with enhanced development workflow",
  "private": true,
  "type": "module",
  "scripts": {
    "rest-tester": "node tools/rest-api-tester/server.mjs"
  },
  "devDependencies": {
  },
  "lint-staged": {
    "*.{js,vue,ts}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{css,scss,sass}": [
      "prettier --write"
    ],
    "*.php": [
      "vendor/bin/pint"
    ]
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "browserslist": [
    "> 1%",
    "last 2 versions",
    "not dead"
  ],
  "dependencies": {
  }
}
